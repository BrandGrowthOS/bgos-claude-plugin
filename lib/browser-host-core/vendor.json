{
  "why": "policy.js, settings.js and profiles.js are byte-identical COPIES of the BGOS agent-browser rule tier, so a daemon-placed agent is judged by exactly the rules the desktop Agent Browser uses AND remembers an owner's answer exactly the way the desktop stores it. A second copy of a permission policy is how two hosts quietly come to disagree about what is sensitive; the sha256 below is what makes the copy checkable instead of merely claimed.",
  "files": {
    "policy.js": {
      "source": "BGOS: frontend/electron-app/agent-browser/policy.js",
      "sha256": "7541a0c32fd5dd1b21ee0e67ba5df11ec7a9fc8e5222e422da8620cbf4f3c6bc"
    },
    "settings.js": {
      "source": "BGOS: frontend/electron-app/agent-browser/settings.js",
      "sha256": "49317db663958dd3441b7bbfc85d3e4961a9d097799b3d0703df0cf7bad5dd72"
    },
    "profiles.js": {
      "source": "BGOS: frontend/electron-app/agent-browser/profiles.js",
      "sha256": "336225827578f3f66849d5d946aebacd6cea877c20cd793fd6c7080d8cd3119a",
      "whyThisOneToo": "It owns WHERE a remembered answer is stored (agentGrants per agent key, the legacy alwaysGrants overlay, withAlwaysGrant, withTrustGrant, withoutTrustGrant). Without it the daemon host would have had to re-implement that shape, and 'Always allow' meaning two different things on two machines is the same class of drift the policy copy exists to prevent."
    }
  },
  "vendoredAt": "2026-09-24, policy.js re vendored 2026-09-24 for the truthful trail (BGOS #1634)",
  "whyADirectory": "settings.js and profiles.js both do require(\"./policy\"), so the copies keep their ORIGINAL filenames and sit together. package.json here pins type=commonjs, because the plugin root declares type=module and these files are CommonJS.",
  "howToUpdate": "Re-copy all three from the BGOS paths above, rerun the hashes, update this file, and run: HOAI_BROWSER_HOST_CORE_SOURCE=<BGOS agent-browser dir> npx tsx --test test/browser-host-core.vendor.test.ts",
  "theHalfThisCannotSee": "Nothing here fires when the BGOS originals change and these copies do not. That check lives on the BGOS side in frontend/electron-app/agent-browser/__tests__/hostCoreVendorPin.test.js, which pins the same two hashes."
}
